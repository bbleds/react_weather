"use strict";

const React = require('react');

const Nav = React.createClass({
  render: function (){
    return (
      <nav>
        <li>Example link one</li>
      </nav>
    )
  }
});

module.exports = Nav;
